---
framework: jest
preRun:
     command:
        - npm ci
preMerge:
  pattern:
    - "./test/*/*.test.js"
postMerge: 
  pattern: 
    - "./test/*/*.test.js"
postRun:
     command:
        - node --version
parallelism: 2
version: 1.0
